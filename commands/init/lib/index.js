'use strict';

function init(projectName, cmdObj, command) {
  // console.log('initzzzz', projectName, command.parent.targetPath)
  // console.log('initzzzz', projectName, process.env.CLI_TARGET_PATH)
}

module.exports = init;